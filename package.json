{
  "name": "snake-game",
  "version": "1.0.0",
  "description": "Snake Game - Rust WASM + TypeScript Frontend",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "server/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/index.js",
    "build:wasm": "cargo build --target wasm32-unknown-unknown --release && wasm-bindgen target/wasm32-unknown-unknown/release/snake_game.wasm --out-dir pkg --target web && cp pkg/*.wasm pkg/*.js pkg/*.d.ts frontend/public/",
    "build:frontend": "cd frontend && npm run build",
    "build": "npm run build:wasm && npm run build:frontend",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "npm run build:wasm && npm run dev:frontend"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "dotenv": "^16.4.5",
    "express": "^5.1.0"
  }
}
