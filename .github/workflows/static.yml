name: Deploy Snake Game

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    environment:
      name: snake-game-prod
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Install wasm-bindgen-cli
        run: |
          if [ ! -f ~/.cargo/bin/wasm-bindgen ]; then
            cargo install wasm-bindgen-cli --version 0.2.118
          fi

      - name: Build WASM
        run: make wasm

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Dependencies
        run: |
          npm install
          cd frontend && npm install

      - name: Build Frontend
        env:
          VITE_BASE_URL: /snake-game/
          VITE_WORLD_WIDTH: ${{ vars.VITE_WORLD_WIDTH || vars.WORLD_WIDTH || 64 }}
          PORT: ${{ vars.PORT || 3000 }}
          NODE_ENV: production
          DEV_SERVER_PORT: ${{ vars.DEV_SERVER_PORT || 8000 }}
          COMPRESSION: ${{ vars.COMPRESSION || 'true' }}
        run: |
          cd frontend && npm run build

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'frontend/dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
